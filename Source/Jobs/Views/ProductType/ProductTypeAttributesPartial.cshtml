@using Presentation.Helper
@model System.Collections.Generic.List<Model.Models.ProductTypeAttribute>
@foreach (var productTypeAttribute in Model)
{
    @Html.Partial("ProductTypeAttributePartial", productTypeAttribute)
}